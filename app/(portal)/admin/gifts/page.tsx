import GiftsManager from "@/components/admin/GiftsManager";

export const dynamic = "force-dynamic";
export const metadata = { title: "Подарки — Админ-панель" };

export default function AdminGiftsPage() {
  return <GiftsManager />;
}
